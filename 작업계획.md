# 🐾 댕냥메디 — 퍼블리싱 → React(Vite + TypeScript) 전환 작업계획서

> **작성일:** 2026-05-28  
> **원본 경로:** `D:\한성주\shoppotofolio\org\`  
> **프레임워크:** React + Vite + TypeScript  
> **스타일:** styled-components  
> **라우터:** React Router v6 (`/`)

---

## 1. 원본 소스 분석 결과

### 1.1 파일 구성
| 파일 | 역할 |
|------|------|
| `index.html` | 메인 페이지 (홈 화면) |
| `style.css` | 전체 스타일 (516줄) |
| `script.js` | 인터랙션 로직 (138줄) |
| `댕냥메디_prd.md` | 서비스 PRD 문서 |

### 1.2 HTML 섹션 구조 분석

```
<div class="app">
  ├── <header class="topbar">         ← 공통 헤더 컴포넌트
  │     ├── 햄버거 메뉴 버튼
  │     ├── 로고 + 브랜드명 (댕냥메디)
  │     └── 검색 버튼
  │
  ├── <main class="scroll-area">      ← 메인 콘텐츠 영역
  │     ├── <section.hero-banner>     ← HeroBanner 컴포넌트
  │     │     ├── 배지, 타이틀, 서브텍스트
  │     │     ├── 상담 버튼 (consultBtn)
  │     │     └── 강아지+고양이 SVG 일러스트
  │     │
  │     ├── <section.symptoms-section> ← SymptomSelector 컴포넌트
  │     │     └── 5개 증상 아이콘 버튼 (관절/눈/피부/면역/구강)
  │     │
  │     ├── <section.guide-section>   ← HealthGuide 컴포넌트
  │     │     ├── 메인 가이드 카드 (mainGuide)
  │     │     └── 서브 카드 2열 (recipeCard, kitCard)
  │     │
  │     └── <section.stats-section>  ← StatsSection 컴포넌트
  │           └── 3개 통계 (건강상담 12k+, 만족도 98%, 24h 모니터링)
  │
  └── <nav class="bottom-nav">        ← 공통 푸터(하단 내비) 컴포넌트
        └── 4개 탭 (홈/건강/클리닉/프로필)
```

### 1.3 JavaScript 기능 분석

| 기능 | 원본 구현 | React 전환 방식 |
|------|-----------|-----------------|
| 토스트 알림 | DOM 조작으로 생성 | `useToast` 커스텀 훅 + Context |
| 증상 버튼 토글 | classList 조작 | `useState`로 activeSymptom 관리 |
| 가이드 카드 클릭 | addEventListener | onClick 핸들러 |
| 하단 탭 전환 | classList 조작 | `useState` 또는 React Router |
| 숫자 카운트업 | requestAnimationFrame | `useEffect` + `useRef` |
| 헤더 스크롤 그림자 | scroll 이벤트 | `useEffect` + scroll 리스너 |

### 1.4 CSS 섹션 분석

| CSS 블록 | 스타일 라인 | 전환 대상 |
|----------|-------------|-----------|
| 리셋 & 기본 | L1~20 | GlobalStyle (createGlobalStyle) |
| 앱 컨테이너 `.app` | L22~31 | AppLayout styled |
| 헤더 `.topbar` | L33~107 | Header.styled.ts |
| 스크롤 영역 | L109~119 | MainLayout styled |
| 히어로 배너 | L130~207 | HeroBanner.styled.ts |
| 증상 선택 | L209~267 | SymptomSelector.styled.ts |
| 건강 가이드 | L269~381 | HealthGuide.styled.ts |
| 통계 | L383~419 | StatsSection.styled.ts |
| 하단 내비 | L421~478 | BottomNav.styled.ts |
| 토스트 | L480~503 | Toast.styled.ts |
| 반응형 | L505~516 | 각 컴포넌트 내 미디어 쿼리 |

---

## 2. 목표 프로젝트 구조

```
D:\한성주\shoppotofolio\
├── org/                          ← 원본 퍼블리싱 소스 (수정 없음)
│   ├── index.html
│   ├── style.css
│   ├── script.js
│   └── 댕냥메디_prd.md
│
└── (프로젝트 루트)/              ← Vite React 프로젝트 생성 위치
    ├── index.html
    ├── vite.config.ts
    ├── tsconfig.json
    ├── package.json
    └── src/
        ├── main.tsx
        ├── App.tsx
        ├── GlobalStyle.ts         ← createGlobalStyle (리셋 + 공통)
        ├── types/
        │   └── index.ts           ← 공통 타입 정의
        ├── hooks/
        │   ├── useToast.ts        ← 토스트 알림 훅
        │   ├── useCountUp.ts      ← 숫자 카운트업 훅
        │   └── useScrollShadow.ts ← 스크롤 헤더 그림자 훅
        ├── context/
        │   └── ToastContext.tsx   ← 전역 토스트 Context
        ├── components/
        │   ├── Header/
        │   │   ├── Header.tsx
        │   │   └── Header.styled.ts
        │   ├── BottomNav/
        │   │   ├── BottomNav.tsx
        │   │   └── BottomNav.styled.ts
        │   ├── Toast/
        │   │   ├── Toast.tsx
        │   │   └── Toast.styled.ts
        │   ├── HeroBanner/
        │   │   ├── HeroBanner.tsx
        │   │   └── HeroBanner.styled.ts
        │   ├── SymptomSelector/
        │   │   ├── SymptomSelector.tsx
        │   │   └── SymptomSelector.styled.ts
        │   ├── HealthGuide/
        │   │   ├── HealthGuide.tsx
        │   │   └── HealthGuide.styled.ts
        │   └── StatsSection/
        │       ├── StatsSection.tsx
        │       └── StatsSection.styled.ts
        └── pages/
            └── HomePage/
                ├── HomePage.tsx
                └── HomePage.styled.ts
```

---

## 3. 컴포넌트 설계 명세

### 3.1 공통 컴포넌트

#### `Header` (`src/components/Header/`)
- **역할:** 상단 고정 헤더
- **포함 요소:**
  - 햄버거 메뉴 버튼 (아이콘 + `aria-label`)
  - SVG 로고 + "댕냥메디" 텍스트
  - 검색 버튼 (SVG 아이콘)
- **Props:** `onScrollShadow?: boolean` (스크롤 시 박스 섀도우 토글)
- **로직:** `useScrollShadow` 훅으로 스크롤 이벤트 감지

#### `BottomNav` (`src/components/BottomNav/`)
- **역할:** 하단 고정 탭 내비게이션
- **포함 요소:** 홈 / 건강 / 클리닉 / 프로필 (각 SVG 아이콘 + 레이블)
- **Props:** `activePage: string`, `onPageChange: (page: string) => void`
- **로직:** React Router `useNavigate` 또는 상태 기반 탭 전환

#### `Toast` (`src/components/Toast/`)
- **역할:** 전역 토스트 알림 표시
- **포함 요소:** 텍스트 메시지, 페이드인/아웃 애니메이션
- **연동:** `ToastContext` + `useToast` 훅으로 전역 관리

---

### 3.2 페이지 전용 컴포넌트

#### `HeroBanner` (`src/components/HeroBanner/`)
- **역할:** 메인 히어로 배너 섹션
- **포함 요소:**
  - 배지 (`🐾 반려동물 건강관리`)
  - 타이틀, 서브텍스트
  - "지금 바로 상담하기" 버튼
  - 강아지 + 고양이 SVG 일러스트 (인라인 SVG 컴포넌트로 분리)
- **Props:** `onConsult: () => void`

#### `SymptomSelector` (`src/components/SymptomSelector/`)
- **역할:** 증상 선택 버튼 목록
- **포함 요소:** 5개 증상 아이콘 버튼 (관절/눈/피부/면역/구강)
- **Props:** `onSelect: (label: string) => void`
- **상태:** `activeSymptom: string | null` (내부 useState)
- **로직:** 선택된 증상 토글, 토스트 메시지 트리거

#### `HealthGuide` (`src/components/HealthGuide/`)
- **역할:** 오늘의 건강 가이드 카드 섹션
- **포함 요소:**
  - 메인 가이드 카드 (태그, 제목, 설명, 이모지 사진)
  - 서브 카드 2개 (레시피, 비상약 체크리스트)
- **Props:** `onCardClick: (cardId: string) => void`

#### `StatsSection` (`src/components/StatsSection/`)
- **역할:** 서비스 통계 수치 표시
- **포함 요소:** 건강 상담(12k+), 만족도(98%), 24h 모니터링
- **로직:** `useCountUp` 훅 + `IntersectionObserver`로 뷰포트 진입 시 카운트업 애니메이션

---

### 3.3 페이지

#### `HomePage` (`src/pages/HomePage/`)
- **라우트:** `/`
- **구성:** 공통 레이아웃(Header + BottomNav) + 4개 섹션 컴포넌트 조합
- **상태 관리:** `useState`로 activePage(탭), activeSymptom 관리
- **토스트 연동:** `useToast` 훅 호출로 각 컴포넌트 이벤트 처리

---

## 4. 스타일 마이그레이션 전략

### 4.1 GlobalStyle (createGlobalStyle)
```typescript
// src/GlobalStyle.ts
// - Google Fonts import (Noto Sans KR)
// - CSS Reset (* { box-sizing: border-box; margin: 0; padding: 0; })
// - html, body 기본 스타일 (background: #f0ece4)
// - body 기본 font-family
```

### 4.2 디자인 토큰 (colors, typography)
```typescript
// src/styles/theme.ts
export const theme = {
  colors: {
    bg: '#f0ece4',
    appBg: '#faf7f2',
    border: '#ede5da',
    text: {
      primary: '#2e1d0e',
      secondary: '#3d2c1e',
      muted: '#9a7a62',
      accent: '#c97b2e',
    },
    hero: {
      gradStart: '#fff8ec',
      gradEnd: '#ffecd2',
    },
    nav: {
      inactive: '#b0a090',
      active: '#c97b2e',
    },
  },
  breakpoint: {
    mobile: '390px',
  },
};
```

### 4.3 컴포넌트별 styled-components 변환 규칙
- 클래스 선택자(`.class`) → `styled.태그`
- 하위 선택자(`.parent .child`) → `${ParentStyled}` 내부 중첩 또는 별도 styled 컴포넌트
- `hover`, `active`, `::before`, `::after` → `&:hover`, `&:active`, `&::before`, `&::after`
- 미디어 쿼리 → `@media (max-width: 390px)` 컴포넌트 내부 포함

---

## 5. 커스텀 훅 설계

### `useToast`
```typescript
// 반환: { showToast: (msg: string) => void }
// 내부: setTimeout으로 2200ms 후 숨김
// Context 연동으로 어디서든 호출 가능
```

### `useCountUp`
```typescript
// 입력: target (number), suffix? (string), enabled (boolean)
// 반환: displayValue (string)
// 내부: requestAnimationFrame + easeOutCubic 이징
// enabled가 true일 때만 실행 (IntersectionObserver 연동)
```

### `useScrollShadow`
```typescript
// 입력: scrollRef (RefObject<HTMLElement>)
// 반환: hasShadow (boolean)
// 내부: scroll 이벤트 리스너 (scrollTop > 4 → true)
```

---

## 6. 단계별 작업 순서

### Phase 1 — 프로젝트 초기 세팅
- [ ] 1-1. Vite + React + TypeScript 프로젝트 생성 (`D:\한성주\shoppotofolio\` 하위)
- [ ] 1-2. 의존성 설치: `react-router-dom`, `styled-components`, `@types/styled-components`
- [ ] 1-3. `vite.config.ts` 설정 (base: `/`, port 설정)
- [ ] 1-4. `tsconfig.json` 경로 별칭 설정 (`@/` → `src/`)
- [ ] 1-5. 폴더 구조 생성 (`components/`, `pages/`, `hooks/`, `context/`, `styles/`, `types/`)
- [ ] 1-6. `GlobalStyle.ts` 작성 (리셋 CSS + Noto Sans KR 임포트)
- [ ] 1-7. `theme.ts` 디자인 토큰 정의

### Phase 2 — 공통 컴포넌트 구현
- [ ] 2-1. `Toast` 컴포넌트 + `ToastContext` + `useToast` 훅
- [ ] 2-2. `Header` 컴포넌트 + `useScrollShadow` 훅
- [ ] 2-3. `BottomNav` 컴포넌트

### Phase 3 — 페이지 전용 컴포넌트 구현
- [ ] 3-1. `HeroBanner` 컴포넌트 (SVG 일러스트 포함)
- [ ] 3-2. `SymptomSelector` 컴포넌트 + `useToast` 연동
- [ ] 3-3. `HealthGuide` 컴포넌트 + `useToast` 연동
- [ ] 3-4. `StatsSection` 컴포넌트 + `useCountUp` 훅 + IntersectionObserver

### Phase 4 — 페이지 조립 및 라우팅
- [ ] 4-1. `HomePage` 페이지 컴포넌트 조립
- [ ] 4-2. `App.tsx` React Router 설정 (route: `/`)
- [ ] 4-3. `main.tsx` 엔트리 포인트 설정 (ThemeProvider, ToastProvider, GlobalStyle)

### Phase 5 — 검증 및 최적화
- [ ] 5-1. 개발 서버 실행 (`npm run dev`) 및 시각적 결과 확인
- [ ] 5-2. 원본 HTML과 React 결과물 UI 비교 검증
- [ ] 5-3. 반응형 확인 (390px 이하 미디어 쿼리)
- [ ] 5-4. 인터랙션 동작 확인 (토스트, 탭 전환, 카운트업, 스크롤 그림자)
- [ ] 5-5. TypeScript 타입 에러 정리

---

## 7. 주요 의존성 목록

| 패키지 | 버전 | 용도 |
|--------|------|------|
| `react` | ^18 | 코어 |
| `react-dom` | ^18 | DOM 렌더링 |
| `react-router-dom` | ^6 | 클라이언트 사이드 라우팅 |
| `styled-components` | ^6 | CSS-in-JS 스타일링 |
| `@types/styled-components` | latest | TypeScript 타입 |
| `vite` | ^5 | 빌드 도구 |
| `@vitejs/plugin-react` | ^4 | Vite React 플러그인 |
| `typescript` | ^5 | TypeScript |

---

## 8. 주의사항 및 고려사항

### 8.1 SVG 처리
- 히어로 배너의 강아지/고양이 복합 SVG는 별도 `PetIllust.tsx` 컴포넌트로 분리
- `aria-hidden="true"` 속성 유지

### 8.2 인라인 스타일 vs styled-components
- 원본의 `style` 속성(예: `scrollArea.style.boxShadow`) 처리:
  - styled-components의 `attrs` 또는 props 기반 동적 스타일로 전환

### 8.3 토스트 타이머 메모리 누수 방지
- `useEffect` cleanup으로 타이머 제거

### 8.4 PRD 기반 향후 확장 고려
- 현재 작업은 메인 페이지(홈) 마이그레이션 기준
- PRD에 명시된 추가 페이지 (건강, 클리닉, 프로필) 는 Phase 4 이후 별도 작업 예정
- Admin / Seller 인터페이스는 별도 라우트로 확장 예정 (`/admin`, `/seller`)

---

## 9. 예상 작업 일정

| Phase | 작업 내용 | 예상 소요 |
|-------|-----------|-----------|
| Phase 1 | 프로젝트 세팅 | 30분 |
| Phase 2 | 공통 컴포넌트 (Header, BottomNav, Toast) | 1시간 |
| Phase 3 | 페이지 컴포넌트 4개 | 1.5시간 |
| Phase 4 | 페이지 조립 + 라우팅 | 30분 |
| Phase 5 | 검증 및 최적화 | 30분 |
| **합계** | | **약 4시간** |
